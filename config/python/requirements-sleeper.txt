# Consolidated Sleeper Detection Requirements
# This file now references the pyproject.toml for all dependencies
#
# Install with: pip install -e packages/sleeper_detection[all]
#
# Available dependency groups:
# - Base (default): Core runtime dependencies only
# - dev: Development and testing tools
# - evaluation: Additional dependencies for full evaluation
# - all: Complete environment with all dependencies
#
# For Docker builds, we install directly from pyproject.toml
# See docker/sleeper-evaluation*.Dockerfile for examples
