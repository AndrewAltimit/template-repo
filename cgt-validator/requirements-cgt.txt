# CGT Validator Requirements
# This file contains all dependencies for the CGT validator project
# which will eventually be moved to its own repository

# Core dependencies
click>=8.0.0
openpyxl>=3.0.0
pandas>=2.0.0
pyyaml>=6.0.0
jsonschema>=4.0.0

# Web scraping
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=4.9.0

# PDF parsing
PyPDF2>=3.0.0
pdfplumber>=0.9.0
tabula-py>=2.8.0

# Reporting
jinja2>=3.1.0
markdown>=3.5.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0

# Utilities
python-dateutil>=2.8.0
tqdm>=4.65.0
tabulate>=0.9.0
colorama>=0.4.6

# Development tools (install separately if needed)
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.0.0
# isort>=5.12.0

# Security scanning (install separately if needed)
# bandit>=1.7.0
# safety>=2.3.0

# Documentation (install separately if needed)
# sphinx>=7.0.0
# sphinx-rtd-theme>=1.3.0
# myst-parser>=2.0.0

# Build tools (install separately if needed)
# build>=1.0.0
# twine>=4.0.0
# pyinstaller>=6.0.0
