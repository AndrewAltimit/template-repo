# Test dependencies for the dashboard unit tests and Selenium E2E tests.
# Used by tests/Dockerfile.test; also installable into the sleeper-eval images,
# so pytest stays within the range required by pytest-asyncio 1.x (pytest>=8.4,<10).
pytest==8.4.2
pytest-html==4.1.1
pytest-cov==7.0.0
pytest-xdist==3.8.0
pytest-timeout==2.4.0

# Selenium and browser automation
selenium==4.38.0
webdriver-manager==4.0.2

# Image processing for visual regression
Pillow==11.1.0
imagehash==4.3.2
numpy==2.2.6

# Data handling
pandas==2.3.2
matplotlib==3.10.6
seaborn==0.13.2

# Dashboard dependencies (for fixtures); versions match dashboard/requirements.txt
streamlit==1.49.1
plotly==6.3.0
bcrypt==4.3.0
reportlab==4.2.5
httpx==0.27.0

# Utilities
python-dotenv==1.1.1
requests==2.32.5
