# Dashboard Configuration
# Copy this file to .env and configure your settings

# Authentication
# Set a secure password for the admin user
DASHBOARD_ADMIN_PASSWORD=admin123

# GPU Orchestrator API Configuration (for Build category)
# GPU Orchestrator API URL (Windows GPU machine)
GPU_API_URL=http://192.168.0.152:8000

# GPU Orchestrator API Key (must match the key set in GPU Orchestrator .env)
GPU_API_KEY=dev-api-key-change-in-production

# Optional Configuration
# DATABASE_PATH=/path/to/evaluation_results.db
# STREAMLIT_DEBUG=1
# CACHE_TTL=3600
