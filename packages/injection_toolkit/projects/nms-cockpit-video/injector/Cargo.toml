[package]
name = "nms-cockpit-injector"
description = "Vulkan texture injection DLL for NMS Cockpit Video Player"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
ash = { workspace = true }
retour = { workspace = true }
windows = { workspace = true }
itk-shmem = { path = "../../../core/itk-shmem" }
itk-ipc = { path = "../../../core/itk-ipc" }
itk-protocol = { path = "../../../core/itk-protocol" }
serde = { workspace = true }
tracing = { workspace = true }
once_cell = "1.19"

[build-dependencies]
naga = { version = "24", features = ["wgsl-in", "spv-out"] }
